# Extension (hands)

TODO: import the igscrapper MV3 extension here, adapted to:
- POST enriched leads straight to the backend (one-click "Send to campaign")
- later: execute DM sends in the real browser session (BrowserChannel)

For now the extension lives in its own repo; its full-enrichment CSV is the
input contract — see ../shared/generator/schema.py (LeadEnrichment).
